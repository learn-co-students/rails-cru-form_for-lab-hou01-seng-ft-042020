<%= form_for(@artist) do |f| %>
  <label>Artist Form</label>

  <label>My artist name</label>
  <%= f.text_field :name %><br>

  <label>bio</label>
  <%= f.text_area :bio %><br>

  <%= f.submit %>

<% end %>